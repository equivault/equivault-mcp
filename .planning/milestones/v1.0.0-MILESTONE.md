# equivault-mcp v1.0.0 — Stable Launch

**Shipped:** 2026-04-17
**Platform tracked:** n/a (docs + release engineering, no new tools)
**Tag:** https://github.com/equivault/equivault-mcp/releases/tag/v1.0.0

## Scope

Freeze the tool surface, add launch-ready documentation, submit to public MCP registries. From v1.0.0 onwards the MCP follows strict semver.

## Tools added

**None.** Tool count stays at 38 — frozen for the v1.x line.

## Docs shipped

- **`CHANGELOG.md`** — Keep-a-Changelog v1.1.0 format. Links every version to the compare view on GitHub. Reverse-chronological from `[Unreleased]` → v1.0.0 → v0.3.0 → v0.2.0 → v0.1.0.
- **`CONTRIBUTING.md`** — project layout, tool-adding workflow (types → TDD test → implement → register → document → changelog), composite-tool pattern (`resolveOrGate`), conventions (ESM `.js` imports, never `console.log`), release flow.
- **`EXAMPLES.md`** — realistic Claude prompts for every one of the 38 tools, grouped by category (core research, fundamental deep-dives, signals & alerts, briefs/media/gurus, daily workflows) with tier-aware behaviour section.
- **README update** — Documentation section added pointing to the three new docs above.

## Release engineering

- `package.json` version bump `0.3.0` → `1.0.0`
- MCP server handshake version bump `0.3.0` → `1.0.0` (`src/index.ts`)
- `git tag v1.0.0 && git push origin v1.0.0`

## Registry submission

[punkpeye/awesome-mcp-servers PR #5036](https://github.com/punkpeye/awesome-mcp-servers/pull/5036) — adds `equivault/equivault-mcp` to the Finance & Fintech section with badges: 🎖️ official, 📇 TypeScript, ☁️ cloud.

## Verification at ship

- All 130 tests pass (unchanged from v0.3.0)
- Typecheck clean
- `npm pack --dry-run` → 22.2 kB, 83 files (build/ + README + LICENSE + package.json)
- CI on main: green across Node 18/20/22
- `Publish to npm` workflow: still failing pending `NPM_TOKEN` secret

## Open item

`NPM_TOKEN` secret must be configured by the operator before any v1.x tag actually publishes to npm. Once configured, re-running the latest failed workflow back-fills v0.1 through v1.0 automatically.
